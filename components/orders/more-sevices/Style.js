import { styled, Box } from "@mui/material";

export const MoreServicesBox = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  margin:"20px 0"
//   my: 5,
});

export const ServiceBox = styled(Box)({
  textAlign: "center",
});
